The program processes a map of 
intersections and edges, handles queries to find the shortest path 
between two points within a radius, and visualizes the graph with the 
computed path. It uses a multi-source Dijkstra's algorithm for 
pathfinding, a custom priority queue for efficiency, and outputs 
results to a text file while visualizing the graph as an image
